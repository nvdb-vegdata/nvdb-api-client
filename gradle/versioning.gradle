apply plugin: 'pl.allegro.tech.build.axion-release'

// http://axion-release-plugin.readthedocs.io/

scmVersion {
    tag {
        prefix = 'nvdb-api-client'
        initialVersion = {config, position -> '1.1.0'}
    }
}

allprojects {
    project.version = scmVersion.version
}
